{
	"name": "burak",
	"version": "1.0.0",
	"description": "this is burak project backend",
	"main": "src/server.ts",
	"scripts": {
		"start": "ts-node src/server.ts",
		"start:dev": "nodemon --exec ts-node src/server.ts",
		"train": "nodemon --exec ts-node train.ts",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kevin",
	"license": "ISC",
	"dependencies": {
		"dotenv": "^16.4.5",
		"moment": "^2.30.1"
	},
	"devDependencies": {
		"nodemon": "^3.0.2",
		"ts-node": "^10.9.1",
		"tsconfig-paths": "^4.2.0",
		"typescript": "^5.2.2"
	}
}
